
import { useMoralis, useNFTBalances, useMoralisWeb3Api  } from 'react-moralis';



const havePassport = () => {
    //const options = { chain: "avax", address: "Our TWRLD ADRESS", token_address: "NFTTOKENADRESS IPFS DATA" };
    //const { getNFTBalance, getNFTBalance2 } = useNFTBalances(options);
    //const { getMoralis } = useMoralis();
    //const { getMoralisWeb3Api } = useMoralisWeb3Api();

    //verify if the user i s authenticated and if the chain is avax
    //const isAuthenticated = getMoralis().isAuthenticated;
    //const chainId = getMoralis().chainId;
    //const isAuthenticatedWeb3 = getMoralisWeb3Api().isAuthenticated;
    //const chainIdWeb3 = getMoralisWeb3Api().chainId;
    //const [loading, setLoading] = React.useState(false);
    //function handleClick() {
    //    setLoading(true);
    //}

    const Redirect = (event) => {
        // if (isAuthenticated && Id === userId === NftId) {
        //     return <Redirect to={'/app'} />;
        // }
    }

    return (
        <div>
            <div>
                <h1>Have Passport</h1>
                <p> You have a passport?</p>
                <p>
                    <button>
                       
                    </button>
                </p>
            </div>
        </div>
)

}

export default havePassport;
